<h1>退会確認</h1>
<p>退会してもよろしいでしょうか？</p>
<div>
<% if member_signed_in? && @mem.id == current_user.id %>
 <%= link_to "退会する", members_hide_path(current_user), method: :put, "data-confirm" => "本当に退会しますか？", class: "btn btn-outline-danger" %>
<% end %>
</div>
<div>
  <%= link_to "退会しない", member_path, class: "btn btn-primary" %>
</div>
