<div class="container">
<div class="col-md-8 offset-md-1">
<h1>注文履歴一覧</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>購入日時</th>
      <th>購入者</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
    <tr>
      <td><%= orders.name %></td>
      <td><%= member.orders.created_at.to_s(:datetime_jp) %></td>
      <td><%= member.orders.received_status %></td>
      <td></td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>