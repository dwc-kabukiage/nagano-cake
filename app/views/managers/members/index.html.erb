<div class="container">
 <div class="row">
  <div class="col-lg-12">
<h4>会員一覧</h4>
<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th>会員ID</th>
      <th>氏名</th>
      <th>メールアドレス</th>
      <th>ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% @members.each do |member| %>
    <tr>
      <td><%= member.id %></td>
      <td><%= link_to member.last_name, managers_member_path(member.id) %>
          <%= link_to member.first_name, managers_member_path(member.id) %>
      </td>
      <td><%= member.email %></td>
      <td><% if member.is_deleted == false %>
          退会
          <% else %>
        　有効
          <% end %>
          </td>
    </tr>
    <% end %>
  </tbody>
</table>
<% paginate @posts %>
  </div>
 </div>
</div>